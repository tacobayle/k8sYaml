apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: web-front-1
spec:
  rules:
    - host: webfront1.vmw.avidemo.fr
      http:
        paths:
          - backend:
              serviceName: web-front1
              servicePort: 80
            path: /