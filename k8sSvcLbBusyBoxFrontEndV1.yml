apiVersion: v1
kind: Service
metadata:
  name: avisvc-lb-web-front1
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      name: eighty
  selector:
    app: web-front1