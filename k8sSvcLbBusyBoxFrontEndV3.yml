apiVersion: v1
kind: Service
metadata:
  name: web3.vmw.avidemo.fr
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      name: eighty
  selector:
    app: web-front3