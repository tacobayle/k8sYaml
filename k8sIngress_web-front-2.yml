apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: web-front-2
spec:
  rules:
    - host: webfront2.vmw.avidemo.fr
      http:
        paths:
          - backend:
              serviceName: web-front2
              servicePort: 80
            path: /