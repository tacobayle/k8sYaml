apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: web-front-3
spec:
  rules:
    - host: webfront3.vmw.avidemo.fr
      http:
        paths:
          - backend:
              serviceName: web-front3
              servicePort: 80
            path: /